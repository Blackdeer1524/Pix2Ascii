#include "argparsing.h"
#include "struct.h"


int argparse(user_params_t *user_params) {
    // flags:
    // -f <Media path>
    // -c : (camera support)
    // -color [sharp | optimal | standard | long] : ascii set
    // -method [average | yuv] : grayscale conversion methods
    return 0;
}
